{% extends "base.twig" %}

{% block content %}

{% if '/my-account/' | is_current_url or '/my-account/orders/' | is_current_url or '/my-account/downloads/' | is_current_url or '/my-account/edit-address/' | is_current_url or '/my-account/edit-account/' | is_current_url or '/my-account/edit-address/billing/' | is_current_url or '/my-account/edit-address/shipping/' | is_current_url %}
<div class="basic-hero basic-hero--dark my-account__hero" style="background-image: url({{ theme.link }}/assets/images/internal-page-bg.jpg);">
    <h1 class="basic-hero__title">{{Post('title')}}</h1>
</div>
{% else %}
	
{% endif %}

	<div class="page content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section class="article-content">
				<!-- <h1 class="article-h1">{{post.title}}</h1> -->
				<div class="article-body">
					{{post.content}}
				</div>
			</section>
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}